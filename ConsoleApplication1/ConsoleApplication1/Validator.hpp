#pragma once

#include<vector>
#include<set>
#include<cmath>
#include<cstdlib>

typedef std::vector<std::vector<bool>> adj_matrix;

struct Validator
{
	static bool is_allowed(adj_matrix & matrix)
	{
		//TODO
		return false;
	}
};